<h2>Open Orders</h2>
<table>
  <tr>
    <th>Order Id</th>
    <th>Product
    <th>Price
    <th>Customer</th>  
  </tr>
  <!--This renders the collection from the controlelr, not the _orders page-->
    <%= render(@orders) || 'No Orders' %>
  </table>

